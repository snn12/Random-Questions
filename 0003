//Giris? h?rf verilir, ?lifbada ondan sonra g?l?n h?rfi mü?yy?n ed?n kod yazin (giris? z v? ya Z verilmir).


#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    
    char ch;
    cin >> ch;
    
    char ch_2 = ch + 1;
    
    cout << ch_2  << endl;

    return 0;
}

