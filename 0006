//Giris? iki int tipind? ?d?d verilir, onlarin böyüyünü çixisa ver?n proqram yazin.

#include <bits/stdc++.h>

using namespace std;

int main (){
	
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	int a,b;
	cin>>a,b; cout<<endl;
	
	cout<<max(a,b)<<endl;
	
	return 0;
}
