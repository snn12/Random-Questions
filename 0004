//Girişə böyük hərf verilir, çıxışa o hərfin kiçiyini verən kod yazın.

#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    
    char ch;
    cin >> ch;
    
    char ch_0 = tolower(ch);
    
    cout << ch_0 << endl;

    return 0;
}
